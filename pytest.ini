[pytest]
# Integration tests hit the real Groq API. They are excluded from the default
# run so `pytest` stays fast, free, and offline. Opt in with: pytest -m integration
addopts = -m "not integration"
markers =
    integration: end-to-end test that calls the real Groq API (needs GROQ_API_KEY + network)
